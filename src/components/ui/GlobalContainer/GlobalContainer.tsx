import { withClassname } from '@/components/hoc';
import { Container } from '@/components/ui';

/**
 * Container that has global horizontal padding.
 */
export const GlobalContainer = withClassname(Container, 'px-[1.6rem]');
